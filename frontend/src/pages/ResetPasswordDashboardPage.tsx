import React from 'react';
import { Box, Typography } from '@mui/material';

const ResetPasswordDashboardPage: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">Reset Password (Dashboard) Page</Typography>
      <Typography>Content for Reset Password within Dashboard will go here.</Typography>
    </Box>
  );
};

export default ResetPasswordDashboardPage;