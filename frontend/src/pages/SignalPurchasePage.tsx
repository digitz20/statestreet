import React from 'react';
import { Box, Typography } from '@mui/material';

const SignalPurchasePage: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">Signal Purchase Page</Typography>
      <Typography>Content for Signal Purchase will go here.</Typography>
    </Box>
  );
};

export default SignalPurchasePage;