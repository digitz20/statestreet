import React from 'react';
import { Box, Typography } from '@mui/material';

const CheckTradePage: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">Check Trade Page</Typography>
      <Typography>Content for Check Trade will go here.</Typography>
    </Box>
  );
};

export default CheckTradePage;